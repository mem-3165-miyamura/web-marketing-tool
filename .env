# ==============================================================================
# マーケティングツール 開発環境設定 (.env)
# Macローカル(npm run dev)からDockerコンテナへ接続する構成
# ==============================================================================

# --- Database (PostgreSQL) ---
# MacからDockerのDBへ繋ぐため localhost を指定
DATABASE_URL="postgresql://admin:password@localhost:5432/marketing_tool?schema=public"
DATABASE_HOST="localhost"
DATABASE_USER="admin"
DATABASE_PASSWORD="password"
DATABASE_NAME="marketing_tool"
DATABASE_PORT="5432"

# Prismaのエラー回避（Mac/Linux両対応のため）
PRISMA_CLIENT_ENGINE_TYPE=binary

# --- Authentication (NextAuth.js) ---
# セッションエラー(CSRF)を防ぐため、実際にアクセスするURLに統一
NEXTAUTH_SECRET="your_very_secret_key_here" 
NEXTAUTH_URL="http://localhost:3001"

# Google Login を使用する場合のみ設定
GOOGLE_CLIENT_ID="YOUR_GOOGLE_CLIENT_ID"
GOOGLE_CLIENT_SECRET="YOUR_GOOGLE_CLIENT_SECRET"

# --- Storage (MinIO / S3) ---
# MacからMinIOの管理画面やAPIへ繋ぐための設定
MINIO_ENDPOINT="http://localhost:9000"
MINIO_ACCESS_KEY="minioadmin"
MINIO_SECRET_KEY="miniopassword"
MINIO_BUCKET_NAME="popups"
MINIO_LOG_BUCKET_NAME="tracking-logs"

# --- Frontend Display ---
# ブラウザが画像を読みに行く際のベースURL
NEXT_PUBLIC_IMAGE_DOMAIN="http://localhost:9000/popups"

# --- Batch Processing (Archive System) ---
CRON_SECRET="your_secure_cron_secret_here"
NEXT_PUBLIC_APP_URL="http://localhost:3001"

# --- Mail Delivery (MailHog) ---
# 送信テスト用。MacからDocker内のMailHogへ
SMTP_HOST="localhost"
SMTP_PORT=1025

# --- Server Port Configuration ---
# Next.jsが起動するポート
PORT=3001